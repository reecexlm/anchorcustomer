apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Release.Name }}-ingress
  namespace: anchor-platform
spec:
  ingressClassName: nginx
  rules:
  - host: sep.localdev.me
    http:
      paths:
      - path: /
        backend:
          service:
            name: sep-server-service
            port:
              number: 8080