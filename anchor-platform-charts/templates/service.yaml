apiVersion: v1
kind: Service
metadata:
  name: {{ template "anchorplatform.fullname" . }}
  labels:
    app: {{ template "anchorplatform.name" . }}
    chart: {{ template "anchorplatform.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "anchorplatform.name" . }}
    release: {{ .Release.Name }}